This example is for a situation where you want to run jobs with the same computational resources (cores, time), just changing the Stata .do file that you want to execute.

This isn't a very common situation.